// 5. Write a function in JavaScript that takes in a number n and returns the
// first n even numbers.

var number = prompt("Enter a number:")

function even(number) {
  if number % 2 === 0 {
    return
  }; else {
    return "please enter a number"
  };
};
